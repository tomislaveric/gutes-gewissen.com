    <IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /_site/index.php [L]
    RewriteCond %{SERVER_PORT} 80
    RewriteRule ^(.*)$ https://www.gutes-gewissen.com/$1 [R,L]
    </IfModule>